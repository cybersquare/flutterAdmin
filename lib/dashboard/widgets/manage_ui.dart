import 'package:flutter/material.dart';

class ManageUI extends StatelessWidget {
  const ManageUI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
      height: 100,
      width: 100,
    );
  }
}
